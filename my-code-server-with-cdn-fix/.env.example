# VS Code Server Docker Environment Configuration
# Copy this file to .env and modify the values as needed

# ============================================================================
# Container Configuration
# ============================================================================

# Container name (default: vscode-server)
CONTAINER_NAME=vscode-server

# Image name for local builds (default: my-code-server-with-cdn-fix:main)
IMAGE_NAME=my-code-server-with-cdn-fix:main

# ============================================================================
# Basic Configuration
# ============================================================================

# Host port mapping (default: 8585)
HOST_PORT=8585

# Host interface to bind to (default: 0.0.0.0)
HOST=0.0.0.0

# Container port (default: 8585)
CONTAINER_PORT=8585

# Connection token for authentication (CHANGE THIS IN PRODUCTION)
TOKEN=change-me-to-secure-token

# ============================================================================
# CDN Configuration Options
# ============================================================================

# CDN Proxy Mode (requires nginx reverse proxy configuration)
# Set to 'true' to use CDN proxy instead of direct CDN access
# Recommended: false (default)
# Use 'true' for production with nginx reverse proxy
USE_CDN_PROXY=false

# CDN Proxy Host (required when USE_CDN_PROXY=true)
# Set to your reverse proxy domain name or IP address
# VS Code Server will use: https://CDN_PROXY_HOST/proxy-unpkg and https://CDN_PROXY_HOST/proxy-cdn
# Example: CDN_PROXY_HOST=my-vscode-server.domain.com
CDN_PROXY_HOST=

# ============================================================================
# User/Group Configuration
# ============================================================================

# User ID for volume permissions (default: 1000)
# Set to match your host user UID: id -u
PUID=1000

# Group ID for volume permissions (default: 998)
# Set to match your host user GID: id -g
PGID=998

# Extra group IDs for additional permissions (comma-separated)
# Adds supplementary groups to the process without changing primary UID/GID
# Example: EXTRA_GID=998 adds access to files owned by GID 998
# Example: EXTRA_GID=998,999 adds access to GIDs 998 and 999
# EXTRA_GID=

# Data directory for persistence (default: ./data)
# Uncomment and set to your preferred path
DATA_DIR=./data
