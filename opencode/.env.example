# OpenCode Docker Environment Configuration
# Copy this file to .env and modify the values as needed

# ============================================================================
# Container Configuration
# ============================================================================

# Container name (default: opencode)
CONTAINER_NAME=opencode

# Image name for local builds (default: opencode:latest)
IMAGE_NAME=opencode:latest

# ============================================================================
# Network Configuration
# ============================================================================

# Host port mapping (default: 4096)
HOST_PORT=4096

# OpenCode server port (default: 4096)
OPENCODE_PORT=4096

# Bind address (default: 0.0.0.0)
OPENCODE_HOST=0.0.0.0

# ============================================================================
# Authentication Configuration
# ============================================================================

# Web UI username (default: opencode)
OPENCODE_SERVER_USERNAME=opencode

# Web UI password (REQUIRED for production)
# Generate a secure password: openssl rand -base64 32
OPENCODE_SERVER_PASSWORD=

# ============================================================================
# User/Group Configuration
# ============================================================================

# User ID for volume permissions (default: 1000)
# Set to match your host user UID: id -u
PUID=1000

# Group ID for volume permissions (default: 998)
# Set to match your host user GID: id -g
PGID=998

# ============================================================================
# Data Directory (Optional)
# ============================================================================

# Data directory for persistence (default: ./data)
# Uncomment and set to your preferred path
DATA_DIR=./data
